import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF1C1F21);
  static const Color object = Color(0xFFFF7100);
  static const Color slash = Color(0xFFFFFFFF);
  static const Color text = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFF2C2F31);
  static const Color border = Color(0xFF3C3F41);
  static const Color correct = Color(0xFF538D4E);
  static const Color accent = Color(0xFFFF7100);
}

class GameConstants {
  static const double minShapeSize = 200.0;
  static const double maxShapeSize = 350.0;
  static const double defaultTargetRatio = 0.5;
  static const int minVertices = 4;
  static const int maxVertices = 8;
}
